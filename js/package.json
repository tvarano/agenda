{
  "name": "js",
  "version": "1.0.0",
  "description": "A scheduler for PVRHSD",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify index.js -o dist/bundle.js",
    "watch": "watchify index.js -o dist/bundle.js -v"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:tvarano54/agenda.git"
  },
  "author": "Thomas Varano",
  "license": "ISC",
  "dependencies": {
    "browserify": "^16.2.3",
    "watchify": "^3.11.1"
  },
  "devDependencies": {
    "@babel/core": "^7.3.3",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^10.0.0"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ]
    ]
  }
}
